<!--miniprogram/pages/near/near.wxml-->
<view class="map">
  <map id="map" longitude="{{ longitude }}" latitude="{{ latitude }}" scale="14"  showLocation markers="{{ markers }}" bindmarkertap="markertap"></map>
</view>
